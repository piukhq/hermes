[uwsgi]
project = hermes
base = /usr/local/src

chdir = %(base)/%(project)

module = %(project).wsgi:application
callable = app

master = true
processes = 1

socket = /tmp/%(project).sock
chmod-socket = 666
vacuum = true
lazy-apps = true
close-on-exec = true
enable-threads = true
